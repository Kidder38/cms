# Produkní nastavení - nastavováno automaticky Heroku
# DATABASE_URL=postgres://user:password@host:port/dbname
# PORT=5000
# NODE_ENV=production

# JWT konfigurace
JWT_SECRET=your_jwt_secret_key_at_least_32_characters
JWT_EXPIRY=24h

# Lokální databáze pro vývoj
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=pujcovna_db

# Frontend URL pro CORS (pouze pro vývoj)
FRONTEND_URL=http://localhost:3000

# Nastavení aplikace
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin123!
DEFAULT_ADMIN_NAME=Administrator